import { MD3LightTheme as DefaultTheme } from 'react-native-paper';

const theme = {
    ...DefaultTheme,
    colors: {
        ...DefaultTheme.colors,
        secondaryContainer: 'orange', // Set your primary color to orange
        // Customize other colors as needed
        primary: 'orange', // Set your primary color to orange

    },
};

export default theme;
